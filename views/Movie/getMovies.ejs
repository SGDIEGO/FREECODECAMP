<%- include("../shared/header") %>
    <h1>MOVIE LIST</h1>

    <div class="movies">

        <div class="movies-principal">
            <% listMovies.forEach(element=> {%>
                <div class="movies-principal__individual">
                    <div>
                        <h3>
                            <%= element.name %>
                        </h3>
                        <div class="movies-individual">
                            <p>
                                <%= element.createdOn %>
                            </p>
                            <p>
                                <%= element.gener %>
                            </p>
                            <p>
                                <%= element.time %>
                            </p>
                            <p>
                                <strong>
                                    <%= element.director %>
                                </strong>
                            </p>
                        </div>
                    </div>
                    <div class="movies-info">
                        <a href="Movie/edit/<%= element.id %>" class="btn btn-secondary">Edit</a>
                        <a href="Movie/info/<%= element.id %>" class="btn btn-info">Info</a>
                        <a href="Movie/delete/<%= element.id %>" class="btn btn-danger">Delete</a>
                    </div>
                </div>
                <% }); %>
        </div>

    </div>

    <%- include("../shared/footer") %>